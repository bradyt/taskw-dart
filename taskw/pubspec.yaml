name: taskw
description: A starting point for Dart libraries or applications.
version: 0.0.0-alpha.0

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  uuid:

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
